# TODO Ledger

This ledger is the authoritative list of active documentation/governance TODOs
for this checkout. It intentionally excludes inherited upstream-era TODO notes
and historical planning ideas unless they have been re-opened for Cuttlefish
with current evidence and exit criteria.

No active documentation TODOs are open after the 2026-06-26 Cuttlefish
documentation stewardship pass.

| ID | Status | Priority | Area | Item | Source | Opened | Last Evidence | Exit Criteria |
|---|---|---|---|---|---|---|---|---|
| STT-CF-001 | open | critical | state-machine | Org change apply route has no pre-state guard — rejected/draft changes can be applied. Add `if (!["pending_approval","approved"].includes(request.status)) → 409` in `gateway/api/routes/org.ts` before calling `applyOrgChange()`. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/api/routes/org.ts:422-438` | POST /apply on rejected change returns 409; POST /apply on approved change succeeds |
| STT-CF-002 | open | critical | state-machine | `markExternalOutboxDelivered()` has no `AND status = 'sending'` WHERE clause — failed/delivered items can be force-transitioned back to delivered. Add status guard to UPDATE. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/sessions/registry/external-outbox.ts:118-131` | Marking a `failed` item delivered leaves it `failed` |
| FSR-CF-001 | open | critical | reliability | `config.yaml` is a hard single point of failure — missing or invalid config hard-crashes with no user-friendly message. Add user-readable error path in `daemon-entry.ts`; separate required vs optional schema fields. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/shared/config.ts:93-113` | Missing config produces readable "run cuttlefish init" message instead of stack trace |
| STT-CF-003 | open | high | state-machine | `markExternalOutboxFailed()` non-atomic read+write of `attempt_count` and no state guard — double-call can corrupt terminal state. Wrap in `db.transaction()` and add `AND status = 'sending'`. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/sessions/registry/external-outbox.ts:135-151` | Concurrent markFailed calls do not double-increment; failed items remain terminal |
| PIP-CF-001 | open | high | reliability | Slack `this.handler(msg)` called sync without try-catch — exceptions silently drop messages. Wrap in try-catch; log at ERROR. Same fix for `app_mention`. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/connectors/slack/index.ts:204,269` | Sync throw in handler logs error and does not surface to Bolt event loop |
| NEG-CF-001 | open | high | concurrency | Concurrent org-change submissions race on HR session creation in `getReusableHrSession()` — two simultaneous requests can both create new sessions. Add module-level mutex or DB UNIQUE constraint. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/hr-steward.ts:306` | Two concurrent submissions reuse or serialize on the same HR session |
| FSR-CF-002 | open | high | reliability | `fs.mkdirSync(cuttlefishHome)` in `gateway/auth.ts` has no try-catch — EACCES produces cryptic error. Wrap with user-readable message. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/auth.ts:45` | Permission error produces readable message including path and next-step hint |
| ARC-CF-001 | open | high | architecture | `SessionManager` is a god object (~800 lines) importing gateway policy, auth, MCP, budget, prompt construction, connector dispatch. Extract `SessionPolicy` and `SessionDispatcher` collaborators. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/sessions/manager.ts` | SessionManager delegates to extractable collaborators; each is independently testable |
| PIP-CF-002 | open | medium | reliability | Email ingest stuck in `dispatching` permanently if `onAutoIngest` throws synchronously before returning a Promise. Guard with `Promise.resolve().then(…).catch(…)`. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/email/service.ts:196-216` | Sync throw in onAutoIngest transitions message to `error` not stuck `dispatching` |
| PIP-CF-003 | open | medium | reliability | HR critique failure auto-applies low-risk org changes via null critique (`.catch()` calls `finishCritique(null)` which proceeds to `applyOrgChange`). On failure, set change status to `error` instead. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/hr-steward.ts:137-143` | Critique session failure blocks auto-apply; change enters `error` state |
| NEG-CF-002 | open | medium | security | Email `allowFrom` trusts raw MIME From header without SPF/DKIM — From header can be forged. Document limitation; check `Authentication-Results` header when available. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/email/normalize.ts`, `packages/cuttlefish/src/email/service.ts:18-30` | Operator docs note SPF/DKIM caveat; Authentication-Results checked if present |
| NEG-CF-004 | open | medium | reliability | Two daemon processes can share the same SQLite DB without a PID lock file. Add `flock`-style exclusive PID lock at startup. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/sessions/registry/core.ts:73-75` | Second daemon instance detects existing lock and refuses to start |
| FSR-CF-003 | open | medium | reliability | Node.js version below minimum is warned but not enforced — daemon child spawns then crashes with unclear error. Either hard-refuse or improve error message. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/lifecycle.ts:53-56` | Running on Node < 24 produces actionable error message; ideally refuses to start |
| ARC-CF-002 | open | medium | architecture | `HrSteward` owns domain logic (change classification) and session dispatch (createSession, dispatchWebSessionRun) — domain/orchestration mixing. Separate via event/callback pattern. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/hr-steward.ts` | HrSteward emits change events; separate listener handles session wiring |
| ARC-CF-003 | open | medium | architecture | `gateway/auth.ts` has four responsibilities: admin token, scoped session token, PTY token, and route denylist. Extract `pty-auth.ts` and `scoped-token.ts`. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/auth.ts` | Auth responsibilities split across purpose-specific modules |
| INV-CF-001 | open | low | invariants | `Approval["type"]` includes `"checkpoint"` but `GET /api/approvals` silently excludes it. Create `ListableApprovalType` alias and document the filter intent. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/shared/types/operations.ts:32`, `packages/cuttlefish/src/gateway/api/routes/approvals.ts:27` | Filter is annotated or type-safe via Exclude<> alias |
| PIP-CF-004 | open | low | reliability | Connector reply failures are fire-and-forget with no audit record — after 2 retries, errors are swallowed silently. Log at WARN with session ID and connector name. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/run-web-session.ts` | Failed reply delivery logged at WARN level with session context |
| NEG-CF-003 | open | low | operator-ux | `autoIngest: true` + empty `allowFrom` silently disables auto-ingest with no operator warning. Log WARN at poll time when this condition is detected. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/email/service.ts:197-198` | Startup/poll log warns operator when autoIngest is on but allowFrom is empty |
| NEG-CF-005 | open | low | reliability | No pre-flight node-pty ABI check at startup — first PTY spawn attempt may fail with cryptic error. Attempt `loadPtyModule()` at startup and log result. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/engines/pty-stream.ts:13-26` | Startup logs PTY availability (success at INFO, failure at WARN); no hard fail |
| ARC-CF-004 | open | low | architecture | Policy mixed with mechanism in the connector-send route — extract `authorizeConnectorSend()` as a pure policy function. | 2026-06-28 structural audit | 2026-06-28 | `packages/cuttlefish/src/gateway/api/routes/connectors.ts:146-157` | Authorization logic is a pure function testable without HTTP context |
