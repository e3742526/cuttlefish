node_modules/
dist/
.next/
.turbo/
coverage/
test-results/
*.tsbuildinfo
.env
.env.local
# npm publish token (cuttlefish-cli) — local only, never commit
packages/cuttlefish/.npmrc
.worktrees/
.DS_Store
.vscode/

# Local agent/tool caches.
.playwright-mcp/
.fissure/
.kilo/

# Giles and local audit/runtime artifacts. These stay local in this public repo
# so they do not confuse readers browsing the source tree.
.giles/
governance/logs/
docs/audits/
docs/logs/
state/
# Anchored to repo root: this ignores the top-level runtime telemetry dir
# (logs/agent-activity.jsonl) without swallowing source dirs named "logs"
# such as packages/web/src/routes/logs/.
/logs/

# Superpowers SDD scratch (briefs, reports, ledger, diffs)
.superpowers/
.aider*
